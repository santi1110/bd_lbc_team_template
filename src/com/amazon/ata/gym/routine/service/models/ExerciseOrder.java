package com.amazon.ata.gym.routine.service.models;

/**
 * Enum representing the possible orders for exercises in a routine.
 */
public enum ExerciseOrder {
    DEFAULT,
    REVERSED,
    RANDOM // Changed SHUFFLED to RANDOM for clarity
}
